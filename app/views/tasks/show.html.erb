<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<% if @task.completed %>
  <% completed = "✔ This task is completed" %>
    <% else %>
  <% completed = "This task is undone." %>
<% end %>
<p><%= completed %></p>

<%= link_to "Delete",
  delete_task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
